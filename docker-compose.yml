services:
  mtc-bot:
    build: .
    container_name: mtc-bot
    env_file:
      - BoktoshiBotModule/.env
      - AsterTradingModule/.env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
